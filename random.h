/*
 * The function randomGaussian generates random integer values with a Gaussian distribution.
 * It takes mean and standard deviation as parameters.
 * The returned values may be negative.
 */

int randomGaussian(int mean, int stddev);
